> xor of continuous numbers have a very special property.
- xor of a even and its next odd that is , xor of e and (e+1) , is 1.
- for f(n) representing contnous xor of numbers from 0 to n , if(n%4==0) then f(n)=n, if (n%4==1) then f(n)=1, if(n%4==2) then f(n)=n+1, if(n%4==3) then f(n)=0





# Nimish and Partitioning [https://codeforces.com/group/NpnwJNnkjM/contest/696453/problem/J]
   FIRST WENT TO SEE COUNT OF BITS AND SEE IF I CAN HOW PARTITION THEM ... ODD COUNT ->ONLY PARITION IN ODD BITS SETS , EVEN CAN ONLY EVEN BIT SETS => ALL CAN BE PARITION INTO SINGLE CHUNK OR ALL SEPARATE..
                -->> WRONG = RACHING NOWHERE

```
    > THEN THOUGH DO WE ACTUALLY NEED TO SEE K-1 CUTS?? -> WHY NOT ONLY 1. (WHY NOT TRY TO FINISH IT IN LIMITED CUTS)
    > NOW OBSERVE IF WE HAVE N COMPONTENTS THENTHERE COMBINED TREE BE CONSIDERED ..
        LET N BE EBVEN -> COMBINED FULL SINGLE TREE HAVE NET XOR 0..
            -> WE COULD BREAK TREE IN A SINGLE CUT INTO 2 PARTS..
        >JUST CHECK IF NET XOR IS 0 OR NOT
        = LET N BE ODD -> COMBINED FULL SINGLE TREE HAVE NET XOR THAT VALUE..
            -> HERE LIKE EVEN CASE .. WE TRY TO USE MINIMUM CUTS (2)=> MAKING 3 COMPONENTS..
                => QUESTION THEN ARISES CAN WE CUT IN 3 PARTS SUCH THAT EACH HAS NET XOR == NET XOR OF WHOLE TREE ???
            > CUT THE SMALLEST SUBTREE FROM THE MAIN TREE IF XOR SAME AS NET XOR , THEN AGAIN DO IT FOR MAJOR CUTTED TREE
    >USE OF THE PROPERTY OF XOR THAT X^X==0 (EVEN TIMES XOR OF SAME). AND X^X^X==X(ODD TIMES XOR OF SAME)
```
> IN XOR IF GETTING MULTIPLE COMPONENTS WITH SAME XOR .. TRY TO COMBINE THEM TOGETHER (INSTEAD OF MULTIPLE .. SINGLE BIG COMPONENT WITH NET VALUE)